/**
 * File:
 *   cfg_mouse.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/sysconfig/mouse
 *   using the sysconfig-agent
 * Access:
 *   read/write
 * Authors:
 *   Unknown <yast2-hacker@suse.de>
 * Example:
 *   Dir(.sysconfig.mouse)
 *   (["DEVICE"])
 **
 *   Read(.sysconfig.mouse.DEVICE)
 *   ("/dev/psaux")
 **
 *   Write(.sysconfig.mouse.DEVICE, "/dev/input/mouse")
 *   (true)
 *
 * $Id$
 *
 * Read/Sets the values defined in <tt>/etc/sysconfig/mouse</tt>
 * in an easy manner.
 */
.sysconfig.mouse

`ag_ini(
  SysConfigFile("/etc/sysconfig/mouse")
)
