/**
 * File:
 *   cfg_gpm.scr
 * Summary:
 *   SCR Agent for reading/writing /etc/sysconfig/gpm
 *   using the sysconfig-agent
 * Access:
 *   read/write
 * Authors:
 *   Unknown <yast2-hacker@suse.de>
 * Example:
 *   Dir(.sysconfig.gpm)
 *   (["DEVICE"])
 **
 *   Read(.sysconfig.gpm.OPTIONS)
 *   ("-t psaux")
 **
 *   Write(.sysconfig.gpm.OPTIONS, "-t ms")
 *   (true)
 *
 * $Id$
 *
 * Read/Sets the values defined in <tt>/etc/sysconfig/gpm</tt>
 * in an easy manner.
 */
.sysconfig.gpm

`ag_ini(
  SCR::SysConfigFile("/etc/sysconfig/gpm")
)
